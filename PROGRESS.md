# PROGRESS.md — Gravegrain build log

State pointer for the autonomous run. The **orchestrator** (main Pi session) owns this file: it records the current phase, per-task attempt counts, what passed, and any blockers. On restart, resume from here.

See `AGENTS.md` → *Autonomous run & escalation protocol* for the rules this log enforces.

---

## Current state

- **🎉 THE MVP IS COMPLETE** (Phases 0–11 all done, planner-verified). Phase 11: 11-1 deterministic RNG, 11-2 chunked/dirty-rect sim (byte-identical), 11-3 LOD+gore settle, 11-4 breach feedback, 11-5 role outfits, 11-6 react-earlier-needs, 11-7 balance+juice — all ✅ & committed.
- **Current work:** post-MVP **Authorized backlog** (user greenlit). ✅ DONE so far: (a) revised death model (extreme→dissolve / quiet→corpse / bite→turn); (b) zombie ladder-climb; (c) plant-a-seed foliage growth (deterministic/chunk-safe); (d) **Warmth + camp vertical slice** (warmth need + roof-based open shelter + fire-warmth + cold-start camp; survivors warm under a roof, leave for water, freeze→corpse if stranded); (e) **✅ Cooperative base-building (Builder/Hauler, GDD §6.2/§8) — COMPLETE & playable end-to-end.** BQ-1 queue module + BQ-2 builder role/hammer/menu + BQ-3 builder construction loop (claim→walk→build→atomic stockpile spend, 6/6) + CB-4 Plan tool (tap-queue/cancel, drag add-only, 9/9) + CB-5 blueprint overlay render (translucent ghosts, reserved=opaque, ctx-only invariant-safe, 9/9) + CB-6 live-loop integration (resetQueue on world init, e2e plan→assign→build through main path, 16/16). MVP simplification: no physical material-hauling sim — atomic stockpile spend satisfies §6.2 for this slice. **NEXT: GDD §14 Beyond** (Diggers/Fisherman roles, iron tier, herd dynamics, dual-edge spawns, tree reproduction, day/night, weather — prioritize with user). All invariants (GATE, chunk byte-equivalence, win/lose, no-tunnel, ImageData==backing) green throughout.
- **Stale-test fixed this session:** `test/p4-t5.test.ts` (GATE point 4) asserted the PRE-revision drowning behaviour (full cell-dissolve). The authorized death-model revision deliberately made drowning a QUIET death → prone corpse (`layDownCorpse`, GDD §5.1; bones stay whole). Refreshed the assertion to `corpse===true && !allBonesDestroyed` — ALL PASS. (Confirmed not a CB regression: test passed at MVP-complete d7a8be9, broke only after death-model 461c3cd+.) Commit 86ed400.
- **USER AUTHORIZATION (this session):** after the MVP, **continue with ALL deferred + added tasks** (the vertical-slice tier is now greenlit). See “Authorized backlog” below and the PLAN.md “Deferred” + playtest sections.
- **NOTE:** GDD was revised externally (commit 6af1cbf — death model: extreme→dissolve, quiet→corpse, bite→turn). Re-read GDD §5.1/§7 before building the death/turn work; it may add a ‘bitten survivor turns into a zombie’ mechanic to the backlog.

## GDD §14 Beyond — user-chosen order (this session)

Tackle in THIS order (user-prioritized): **1. Weather (§10) ✅ DONE (VS-1) → 2. Tree reproduction (§9) ✅ DONE (eco-repro entry below) → 3. Herd dynamics (§7.1) ✅ DONE ON BRANCH `herd-dynamics` (f26c456) — DO NOT RE-IMPLEMENT, `git merge herd-dynamics` at your next clean-tree task boundary (built+verified in a parallel session: herdPullX idle-goal bias in zombie.ts, test/herd.test.ts 14/14, full sweep 87/87 on that branch; only config.ts overlaps your in-flight work and hunks don't collide) → 4. Diggers/Fisherman roles (§6.2) → 5. Iron tools (§6.3) → 6. Dual-edge spawns (§7.1).** Same loop per item: planner decomposes → route per policy → verify Done-when → commit per item. Re-read the named GDD § each item. Keep all invariants green (GATE, chunk byte-equivalence, win/lose, no-tunnel, ImageData==backing).

- **Weather context for the planner:** the Warmth slice already ships a WARMTH need driven by a GLOBAL `AMBIENT_COLD=true` flag (always cold). config.ts comments explicitly DEFER "§10 ambient temperature fields + day/night cycles" to "a later warmth task" — weather is that task. Weather should make ambient temperature/conditions DYNAMIC (clear/rain/snow), add rain (fills water, douses fire, grows plants), snow (accumulation cells, sharper cold), and feed the existing WARMTH need — NOT rebuild warmth. New SNOW material + rain/fire/plant interactions = sim-correctness → expensive_coder for the core.

## Authorized backlog (post-MVP — do after Phase 11, in roughly this order)

1. **Finish Phase 11:** 11-5 role outfits, 11-6 react-earlier needs, 11-7 balance/juice. (cheap)
2. **Bite-to-turn death model (NEW, GDD 6af1cbf):** re-read revised GDD §5.1/§7; implement extreme-damage→dissolve / quiet-death→corpse / bite→survivor turns zombie. (expensive — touches damage/zombie/survivor.)
3. **Zombie ladder-climb (v0.5 #A):** crowded zombies use each other as standable ground to climb walls; keep no-tunnel + perf cap. (expensive — zombie AI/locomotion, Phase-7 follow-up.)
4. **Plant-a-seed foliage growth (v0.6 #G):** SAPLING material the player plants → grows into FOLIAGE over GROW_TICKS on soil; replace free Foliage paint with a Plant tool. (expensive — new sim rule.)
5. **Vertical slice — Warmth + camp (deferred):** Warmth need + temperature + fire-as-warmth + Shelter structure (GDD §6.1/§10/§8). (expensive, multi-task.)
6. **Vertical slice — cooperative base-building:** survivor-driven construction / Builder-Hauler role (GDD §6.2). (expensive, multi-task.)
7. **Further vertical-slice/Beyond (GDD §14):** Diggers/Fisherman roles, iron tool tier + upgrades, herd dynamics, dual-edge spawns, tree reproduction, day/night, weather — prioritize with the user.

_Process for the backlog: same loop — planner decomposes each item into testable tasks; route per policy; verify Done-when; commit per item. Re-read the (revised) GDD sections each item names. THE GATE stays cleared; keep regression suites + smoke green._
- **Last passed Done-when:** Phase 10 — landscape prompt + touch-sized UI + pinch/wheel zoom (correct screen↔world, no hi-DPI regression) + tap/drag/long-press + long-press/right-click role menu + tap-to-cycle + responsive resize + gore/zombie perf caps. Planner-verified PASS; Phases 3–9 not regressed. MANUAL on-device framerate check still recommended.
- **Phase 11 should fold in the deferred playtest items:** v0.5 #C breach VISUAL feedback (crack/darken cells by integrity), v0.5 #D wave pacing (done), v0.6 #E role outfits (tint body by role for readability), v0.5 #B ammo (done), in-scope 'react earlier to needs' sliver, plus chunking/LOD/juice/balance. Deferred vertical-slice (camp/shelter/warmth, cooperative building, plant-a-seed growth) stays OUT unless authorised.
- **v0.4b playtest patches (committed, between Phase 9 and 10):** hi-DPI viewport corner FIXED (render CSS px); blank-view/empty-sky FIXED (vertical surface framing + horizontal center on colony); zombies-don't-move FIXED (idle drift toward colony, idle speed 0.12→0.2); zombies-buried-at-edge FIXED (spawn inset + on actual surface); sand/dirt-stratify FIXED (powders only displace AIR/fluid). Commits e777873, eabd9b1, 6371130, 590931e, d0b1bf3.
- **SCOPE DECISION (user):** continue per PLAN (finish MVP). Camp/shelter/Warmth/fire-warmth/cooperative-building DEFERRED to post-MVP vertical slice (logged in PLAN.md). In-scope 'react earlier to needs' sliver → Phase 11 balance.
- **Last passed Done-when:** Phase 9 — fresh seed plays the full loop on a layered procedural world; survivors forage/drink natural resources; escalating waves from one edge; win (survive 5 waves) / lose (all dead) with end screen; off-screen awareness (death-cause toasts w/ ←→, edge arrows, minimap, speed toggle). Planner-verified PASS, incl. all 7 v0.4 playtest fixes. Phases 3–8 not regressed.
- **GATE:** cleared. **Phase 10 carry-forwards / balance:** (a) ~~dpr putImageData corner bug~~ FIXED + ~~camera framed empty sky (world taller than viewport)~~ FIXED (both user-reported): render in CSS px (canvas backing=floor(rect.w/h), no dpr) so ImageData==backing; and clampCamera no longer pins y=0 — main frames camera vertically on the surface (spawnY ~30% down) after worldgen + on resize. Verified headless: ImageData fills canvas, 63% terrain in-frame. Later refinement: crisp-retina device-px render; optional vertical scroll input to view deep ore. (b) Balance: zombie spawn edge ~920 cells from colony → sparse early combat. (c) miner targets any exposed STONE/WALL — fine in worldgen (DIRT surface).
- **Playtest v0.4 fixes (all landed in Phase 9):** #1 death toasts+needs bars, #2 STEP_UP_MAX=2, #3 nearest-REACHABLE resource targeting, #4 generateWorld wired, #5 reachable ore, #6/#7 removed free Wood/Stone paint (build via costed/breachable Fence/Wall).
- **Last passed Done-when:** Phase 7 — zombies wander in from one edge, lock onto survivors, a guard legs the front rank then headshots crawlers, and a mob claws through a wooden fence (pursuit-driven breach in 155 ticks; pressure scales 2.25×). Damage uses the GATE handoff (real cells, no HP). Planner-verified (1 retry: fixed a breach mis-aim where the body's overhanging arm probed past a short fence). Build + smoke + dev green.
- **GATE:** cleared. Routing = normal policy.
- **Phase 8 note:** breaching is generic over hasIntegrity — stone WALLS placed in Phase 8 (with integrity) will be breachable by the same code unchanged. Keep walls tall enough OR rely on the per-row breach probe (now robust to short structures).
- **Stale-test note:** test/p7-t2.test.ts R2/R3 assertions are stale (written when updateZombie was movement-only; the now-wired adjacent-strike skews its pursuit-speed/monotonicity numbers). Not a production defect; refresh opportunistically.
- **Last passed Done-when:** Phase 6 — assign a lumberjack → walks THROUGH trees, chops for wood, returns to pile, axe breaks @5 chops; assignment gated by tool/stockpile. Planner-verified PASS; Phase 3/4/5 regression intact. (Also fixed a runtime bootstrap init-order bug + added a DOM-stub smoke-test guard.)
- **GATE:** cleared. Routing = normal policy. Phase 7 (zombie AI / combat / breaching) is core sim → expensive_coder.
- **Lesson:** HTTP-200 ≠ the page runs. Runtime DOM bootstrap is now guarded by test/main-smoke.test.ts — keep it passing; have coders run it when they touch main.ts/renderer/input.
- **Last passed Done-when:** Phase 5 — autonomous survivors wander, drink/eat at need thresholds (path adjacent to water/foliage, never into them), die via Phase-4 dissolve when no resource; A* on coarse navgrid + local-only path invalidation; no tunnelling. Planner-verified PASS. (Fixed a latent locomotion infinite-loop: horizontal flush now whole-cell threshold 1.)
- **GATE:** cleared (Phases 0–4). Routing = normal policy.
- **Phase 6 note:** wire FOLIAGE permeableToBodies (bodies walk THROUGH foliage; chopping is a separate harvest) — and fix the now-stale 'foliage is permeable' comment in main.ts/survivor.ts that currently contradicts the collide-and-path-adjacent behaviour.
- **Carry-forward (Phase 10):** renderer builds a CSS-px ImageData while main.ts does ctx.scale(dpr); putImageData ignores the transform, so on dpr>1 the world draws into a corner. Fix in Phase 10 (devicePixelRatio render path).
- **THE GATE:** not reached (locked — Phases 0–4 must pass before any Phase 5+ work)
- **Run mode:** unattended / set-and-forget

---

## Phase checklist

- [x] Phase 0 — Scaffold, render loop & camera
- [x] Phase 1 — Falling-sand core
- [x] Phase 2 — Materials, fire, interactions, integrity
- [x] Phase 3 — Hybrid body locomotion **(GATE part 1)**
- [x] Phase 4 — Damage→cells handoff **(GATE CLEARED ✅)**
- [x] Phase 5 — Survivors: needs + autonomy + pathing
- [x] Phase 6 — Roles, resources, wood-tier tools
- [x] Phase 7 — Zombies, combat, breaching
- [x] Phase 8 — Player building + fire-as-tool
- [x] Phase 9 — Worldgen, waves, win/lose, UI
- [x] Phase 10 — Mobile & touch polish
- [ ] Phase 1 — Falling-sand core
- [ ] Phase 2 — Materials, fire, interactions, integrity
- [ ] Phase 3 — Hybrid body locomotion **(GATE)**
- [ ] Phase 4 — Damage→cells handoff **(GATE)** ← prove the illusion before anything below
- [ ] Phase 5 — Survivors: needs + autonomy + pathing
- [ ] Phase 6 — Roles, resources, wood-tier tools
- [ ] Phase 7 — Zombies, combat, breaching
- [ ] Phase 8 — Player building + fire-as-tool
- [ ] Phase 9 — Worldgen, waves, win/lose, UI
- [ ] Phase 10 — Mobile & touch polish
- [x] Phase 11 — Performance & feel pass  — **MVP COMPLETE 🎉**

---

## Task log

Append one row per coder return. Format:

`<phase> · <task-id> · <route> · attempt N/2 · pass|fail · <one-line note>`

Escalations: when `cheap_coder` fails 2/2 (or a task is revealed to touch sim/GATE/perf), the orchestrator re-spawns on `expensive_coder` and continues the count there. After `expensive_coder` fails 2/2, write a `BLOCKED:` row and pause for review.

Phase 0 · planner · decomposed into p0-t1 (scaffold, gates all), t2 config, t3 grid, t4 camera, t5 renderer, t6 main loop, t7 input/pan — all routed cheap_coder.
ENV NOTE · cheap_coder's configured model `openai/gpt-5-codex` is NOT authed in this sandbox (spawns return 0 tool uses, no output — silent no-op, as KICKOFF warned). The per-spawn model override does NOT apply to custom agents (frontmatter wins), so I edited `.pi/agents/cheap_coder.md` to repoint its model to `anthropic/claude-haiku-4-5` (confirmed authed — Explore uses it). expensive_coder (opus) and planner (opus) work. Also: git absent from base VM image → installed via apk; git needs `export HOME=/workspace` (global config lives there).

Phase 0 · p0-t1 · cheap_coder(haiku) · attempt 1/1 · pass · Vite+TS-strict scaffold; npm build green.
Phase 0 · p0-t2..t7 · cheap_coder(haiku) · attempt 1/1 · pass · config, grid (2x Uint8Array), camera (clamp+round-trip), visible-window renderer (FPS,dpr), fixed-timestep loop (pause P, step ./]), pointer-first drag-pan (touch-action:none). Orchestrator fixed a pan-runaway bug in input.ts (anchor not reset on move). Build + dev-server (HTTP 200) verified.

ENV RECOVERY (mid-Phase-1) · A coder's `npm install` triggered the VM's broken global npm (truncated filenames in @sigstore, dropped rollup/esbuild musl binaries) — dev server crashed. Fix: installed standalone **pnpm** (persisted at `/workspace/.tooling/pnpm`, wrapper `/usr/local/bin/pnpm`), reinstalled deps with correct musl rollup/esbuild. Mount has no exec bit → rewrote package.json scripts to call binaries via `node ./node_modules/<pkg>/...`. Documented in `TOOLING.md`; added "never npm install" warnings to both coder agents. `npm run build`/`dev` work again.

Phase 1 · p1-t1 · cheap_coder(haiku) · 1/1 · pass · materials.ts (AIR/SAND/STONE/WATER table+helpers) + config densities/BRUSH_RADIUS/GRAVITY_STEPS.
Phase 1 · p1-t2 · cheap_coder(haiku) · 1/1 · pass · renderer palette from MATERIALS, pre-parsed RGB.
Phase 1 · p1-t3 · expensive_coder(opus) · 1/1 · pass · sand sim: bottom-up scan, scan-flip, angle of repose; headless 10/10 (mass-conserved, no tunnel, stable pile, no bias).
Phase 1 · p1-t4 · expensive_coder(opus) · 1/1 · pass · water seek-level + general density swap + explicit moved-guard; headless verified (water flattens, sand sinks, masses conserved, single-cell moves, 0.88ms/tick).
Phase 1 · p1-t5 · cheap_coder(haiku) · 1/1 · pass · pan-vs-paint toolbar (Pan/Sand/Stone/Water/Erase), brush disc, single Pointer-Events path. (Agent returned no summary but work landed; orchestrator verified.)
Phase 1 · VERIFY · planner · PASS · sim correctness + mode coherence + MVP scope confirmed; flagged dpr putImageData issue for Phase 10.

Phase 2 · p2-t1 · cheap_coder(haiku) · 1/1 · pass · MATERIALS extended to 11 (DIRT/ORE/WOOD/FOLIAGE/FIRE/SMOKE/ASH) + config densities/integrity; 11/11 headless.
Phase 2 · p2-t2 · cheap_coder(haiku) · 1/1 · pass · grid.placeMaterial seeds baseIntegrity; paint routed through it; 4/4 headless.
Phase 2 · p2-t3 · expensive_coder(opus) · 1/1 · pass · updateDirt (steeper via DIRT_SPILL_CHANCE) + updateAsh (inert powder); dirt footprint 77 < sand 79, mass conserved, no tunnel.
Phase 2 · p2-t4 · expensive_coder(opus) · 1/1 · pass · updateGas: SMOKE rises (meanY 229→169), ceiling blocks, dissipates in 176 ticks, no double-move (moved-guard for upward motion).
Phase 2 · p2-t5 · expensive_coder(opus) · 1/1 · pass · updateFire: lifetime in integrity slot, spread, →ash/smoke, full burnout (peak 396, ash 274, out by tick91).
Phase 2 · p2-t6 · expensive_coder(opus) · 1/1 · pass · reactions.ts water+fire→steam (watered dies in 1 tick vs 60); collapse via existing fall rule; shared ignite() helper. (Left a test/ harness — isolated, doesn't affect build.)
Phase 2 · p2-t7 · cheap_coder(haiku) · 1/1 · pass · toolbar Dirt/Wood/Foliage + Ignite tool via tryIgnite→ignite (flammable-only); 9/9 unit.
Phase 2 · VERIFY · planner · PASS · end-to-end burn+douse over real modules; MVP scope clean (no FLESH/BONE/BLOOD, no breaching/zombies, no Ice/Snow).

Phase 3 (GATE pt1) · p3-t1 · expensive_coder(opus) · 1/1 · pass · body.ts: Body/Bone/BodyPixel + createBody (52-px 6×12 humanoid, 6 bones, feet-centre anchor, unique cells); config seeds.
Phase 3 · p3-t2 · expensive_coder(opus) · 1/1 · pass · locomotion.ts ground-probe + swept fall + isSolidForBody; rests on floor/pit, no-tunnel every tick.
Phase 3 · p3-t3 · expensive_coder(opus) · 1/1 · pass · walk + step-up(≤STEP_UP_MAX) + wall-stop via xRemainder; climbs 1-cell step, stops at 3-cell wall, falls into pit; no-tunnel.
Phase 3 · p3-t4 · cheap_coder(haiku) · 1/1 · pass · renderer setBody + draw bone pixels at cell resolution; worldToScreen alignment === cell layer (pixel-perfect, camera-locked).
Phase 3 · p3-t5 · cheap_coder(haiku) · 1/1 · pass · main.ts wires createBody+setBody+updateBody (after sim.step); arrow driver + stall-flip pacer; test terrain (step+pit). (Agent returned no summary; work landed, orchestrator verified build+dev.)
Phase 3 · VERIFY · planner · PASS · full Done-when over real modules; no-tunnel held ~1500 ticks; alignment/illusion confirmed; MVP scope clean. THE GATE part 1 met.

Phase 4 (THE GATE) · p4-t1 · expensive_coder(opus) · 1/1 · pass · FLESH/BONE/BLOOD materials + BodyPixel.material; pixel colour from material; reactions key on WATER (blood douses nothing); 37 FLESH/15 BONE.
Phase 4 · p4-t2 · expensive_coder(opus) · 1/1 · pass · sim rules for FLESH/BONE (powderFall) + BLOOD (water-like); damage.ts releaseBone (floor never overwritten, cells fall/pile, idempotent).
Phase 4 · p4-t3 · expensive_coder(opus) · 1/1 · pass · applyDamage dispatch + dissolveBody; head→52/52 pile; leg/arm flags; torso threshold; displaceable narrowed to AIR|fluid (terrain never deleted).
Phase 4 · p4-t4 · expensive_coder(opus) · 1/1 · pass · crawl @CRAWL_SPEED (ratio 0.40), dead-body guard, no-tunnel 500t.
Phase 4 · p4-t5 · expensive_coder(opus) · 1/1 · pass · drown @DROWN_TICKS=180 (died tick179) + sand/dirt head-pin (0 vs 18); reactToEnvironment.
Phase 4 · p4-t6 · expensive_coder(opus) · attempt1 no-op (interrupted), attempt2/2 pass · FLESH burns like wood; living body catches fire (checkFire) & dissolves; no spurious ignition.
Phase 4 · p4-t7 · expensive_coder(opus) · 1/1 · pass · Shoot tool + pick.ts pickBone (DOM-free, unit-tested); setTargetBody; gate hand-runnable.
Phase 4 · VERIFY (THE GATE) · planner · PASS · all 5 points over real modules; zero illusion seam; 1.41ms/tick worst-case. ARCHITECTURE PROVEN — Phase 5 unlocked.

Phase 5 · p5-t1 · cheap_coder(haiku) · 1/1 · pass · 17 config seeds (needs/wander/nav).
Phase 5 · p5-t2 · expensive_coder(opus) · 1/1 · pass · navgrid.ts + pathfinding.ts: coarse A*, walkability w/ headroom, local-only isPathStale (on-path edit→stale, far edit→not).
Phase 5 · p5-t3 · expensive_coder(opus) · 1/1 · pass · survivor.ts needs deplete (exertion/heat) + bounded wander + death via dissolveBody. (Agent result lost on cleanup; orchestrator headless-verified: idle drop 6/9, wander maxDist 30, dies thirst@~6667.) ORCHESTRATOR FIX: diagnosed+fixed locomotion.ts horizontal-flush infinite loop (threshold 0.5 + whole-unit decrement oscillated at ±0.5 → changed to whole-cell threshold 1); Phase 3/4 regression re-run clean.
Phase 5 · p5-t4 · expensive_coder(opus) · 1/1 · pass · auto-override: fleeFire>seekWater>seekFood>wander; A* path adjacent to resource + local steering; restore on arrival (eat consumes foliage+markTerrainEdit); repaths on isPathStale. 5/5 incl. no-foliage-overlap, no-resource death, repath-on-edit.
Phase 5 · p5-t5 · cheap_coder(haiku) · 1/1 · pass · multi-body renderer (setBodies); main spawns 4 survivors, rebuildNavgrid at startup, updateSurvivor each; pacer retired. 4 survivors recover needs independently, no tunnel.
Phase 5 · VERIFY · planner · PASS · 17/17 end-to-end (wander/drink/eat/die + path locality + no-tunnel + dev no-hang); MVP scope clean; flagged stale foliage-permeable comment for Phase 6.

Phase 6 · p6-t1 · expensive_coder(opus) · 1/1 · pass · isSolidForBody honours permeableToBodies (bodies walk THROUGH foliage; fall through foliage-only columns); full Phase 2–5 suite regression green; fixed stale comments.
Phase 6 · p6-t2 · cheap_coder(haiku) · 1/1 · pass · resources.ts global stockpile {wood,stone,food,ore} add/canAfford/spend(atomic)/stockpilePoint; 24 assertions.
Phase 6 · p6-t3 · expensive_coder(opus) · 1/1 · pass · roles.ts: 4 roles, wood tools+durability(5)/break, canAssign/craftToolFor gating, findTarget (miner picks EXPOSED rock, skips buried); config seeds.
Phase 6 · p6-t4 · expensive_coder(opus) · 1/1 · pass · survivor.ts role loop find→path→work→deposit→repeat; needs override preempts then resumes; tool break→idle; lumberjack deposits wood, axe breaks @5 chops, miner mines stone; no-tunnel.
FIX · orchestrator · main.ts bootstrap init-order bug (user-reported blank screen + dead buttons + 'Renderer not initialized'): resizeCanvas()/getRenderer() ran before initRenderer()/initInput(). Reordered init before first resize. Added test/main-smoke.test.ts (DOM-stub) as a permanent runtime-bootstrap regression guard — catches the class of error HTTP-200/headless-module checks miss. Build+dev(HTTP 200) verified.
Phase 6 · p6-t5 · cheap_coder(haiku) · 1/1 · pass · Assign tool: tap survivor→role menu (greyed via canAssign), assignRole; stockpile HUD; main seeds forest + exposed stone/ORE + STARTING_WOOD + setStockpilePoint/setSurvivors. Smoke test still passes (init order intact).
Phase 6 · VERIFY · planner · PASS · end-to-end over real modules: gating both ways, walk-through-foliage (no STONE tunnel), chop→deposit (first @tick229), axe breaks @5 chops→idle; MVP scope clean; Phase 3/4/5 regression intact.

Phase 7 · p7-t1 · cheap_coder(haiku) · 1/1 · pass · 16 config seeds (sense/speeds/attack/breach/waves), emergent-damage note (no HP).
Phase 7 · p7-t2 · expensive_coder(opus) · 1/1 · pass · zombie.ts idle meander + detect→attack pursuit via navgrid + sub-cell speed gate; idle bounded, gap closes, no tunnel. (attack speed caps at WALK_SPEED — locomotion step fixed; acceptable.)
Phase 7 · p7-t3 · expensive_coder(opus) · 1/1 · pass · combat.ts (bodiesAdjacent/pickAttackRegion/meleeAttack→applyDamage); zombie strike releases real cells, cooldown respected, death via dissolve.
Phase 7 · p7-t4 · expensive_coder(opus) · 1/1 · pass · guard combat: legs intact zombie (@26)→crawl, headshots crawler (@71)→death; needs/fire override preempts; non-guards don't attack.
Phase 7 · p7-t5 · expensive_coder(opus) · attempt2/2 · pass · breaching.ts integrity chip + crowd pressure; FIXED breach mis-aim (per-row leading-edge probe vs overhanging arm) — pursuit mob breaches 4-tall fence @155t, n=4 2.25× faster, stone never chipped.
Phase 7 · p7-t6 · cheap_coder(haiku) · 1/1 · pass · waves.ts one-edge escalating staggered spawner (3→5), MAX_ZOMBIES cap.
Phase 7 · p7-t7 · expensive_coder(opus) · 1/1 · pass · main integration (step→updateZombie→updateSurvivor(zombies)→resolveBreaching→updateWaves), green-tint zombies, Shoot-hits-zombies, fence+guard scene; smoke test green.
Phase 7 · VERIFY · planner · FAIL·retry (breach mis-aim) → fixed → PASS · all 5 clauses over real modules (wander→lock, leg+headshot, fence breach pursuit-driven, GATE damage, no-tunnel); MVP scope clean; Phases 3–6 not regressed.

Phase 8 · 8-1 · expensive_coder(opus) · 1/1 · pass · WALL material (id14, integrity200, solid, non-flammable) + config (WALL/FENCE_INTEGRITY, costs, STARTING_STONE); raw STONE stays non-breachable.
Phase 8 · 8-2 · expensive_coder(opus) · 1/1 · pass · building.ts placeStructure (atomic inBounds→spend→placeMaterial→markTerrainEdit), canPlace/structureCost; scarcity + navgrid-bump verified.
Phase 8 · 8-3 · cheap_coder(haiku) · 1/1 · pass · toolbar Fence/Wall + input Build mode (single-cell drag = line); smoke test green.
Phase 8 · 8-4 · cheap_coder(haiku) · 1/1 · pass · main seeds STARTING_STONE; refreshBuildButtons greys unaffordable each frame (stub-safe); smoke green.
Phase 8 · 8-5 · cheap_coder(haiku) · 1/1 · pass · test/phase8-building.test.ts: scarcity, WALL chips vs raw STONE immune, fence breaches 3.14× faster than wall, fire spreads fence-to-fence. All 4 PASS.
Phase 8 · VERIFY · planner · PASS · wall-off chokepoint (path→null), fire trap catches herd body-to-body (3 zombies @staggered ticks, 1 died to fire), fence catches/wall doesn't, scarcity enforced; MVP clean; Phases 4–7 not regressed.

Phase 9 · 9-1 · cheap(haiku) · pass · 22 config seeds (worldgen/wave/win/speed).
Phase 9 · 9-2 · expensive(opus) · pass · worldgen.ts deterministic layered map + spawn-zone guarantees (mulberry32; foliage418/water234 near spawn, 920 from edge).
Phase 9 · 9-3 · cheap(haiku) · pass · waves curve (5 waves, sizes 3→11, intervals 1200→800) + allWavesCleared.
Phase 9 · 9-4 · cheap(haiku) · pass · state.ts win/lose latch + per-death cause watcher.
Phase 9 · 9-5 · cheap(haiku) · pass · ui.ts needs bars, death toasts, end screen, 1×/2×/3× speed.
Phase 9 · 9-6 · cheap(haiku) · pass · ui edge arrows + minimap + camera.jumpCameraTo + minimap-click jump (round-trip+clamp verified).
Phase 9 · PLAYTEST · orchestrator direct · #2 STEP_UP_MAX 1→2 (regressions pass); #6/#7 removed free Wood/Stone paint buttons (smoke green).
Phase 9 · 9-8 · expensive(opus) · pass · #3/#5 nearest-REACHABLE targeting (forage worldgen bush @65t, drink reachable pond not sealed, miner reaches ore); p5-t4/p6 regressions pass.
Phase 9 · 9-7 · expensive(opus) · pass · main integration: generateWorld+rebuildNavgrid, gameState loop (freeze on end), UI overlays, speed loop, direction death toasts; 4 survivors live 3000t, loss state reached; smoke+dev green.
Phase 9 · VERIFY · planner · PASS · full-loop win/lose + off-screen awareness + all 7 playtest fixes over real modules (layered world, forage/drink reachable, miner ore, climb-2/stop-4, WALL breached vs raw STONE immune, no free wood/stone paint); MVP clean; Phases 3–8 intact.

v0.4b/v0.5/v0.6 playtest patches (between phases, committed): hi-DPI corner; blank-view vertical framing + center-on-colony; zombies idle-drift + spawn-on-surface; sand/dirt no-stratify; limited ammo (STARTING_AMMO=15); wave pacing 1200→2400; Wood/Stone button relabel.
Phase 10 · 10-1/2 · cheap(haiku) · pass · landscape rotate prompt + touch-sized (≥44px) hover-free responsive toolbar.
Phase 10 · 10-3 · expensive(opus) · pass · camera.zoom (effectiveCellPx) + setZoom-about-anchor wired through screen↔world+renderer; ImageData==backing invariant kept; 95/0.
Phase 10 · 10-4 · cheap(haiku) · pass · pointer registry + classifyGesture (tap/drag/longpress); Shoot/Assign on tap; 6/6.
Phase 10 · 10-5 · cheap(haiku) · pass · pinch + wheel zoom; 2-finger suppresses tools; 12/12.
Phase 10 · 10-6 + #F · cheap(haiku) · pass · long-press + right-click role menu; pickCycling tap-to-cycle; 12/12.
Phase 10 · 10-7 · cheap(haiku) · pass · orientationchange resize; canvas==viewport invariant 20/20.
Phase 10 · 10-8 · expensive(opus) · pass · sweepGore caps loose debris @MAX_GORE_CELLS (never terrain); MAX_ZOMBIES=24; gore 10460→1500, p4 regressions pass.
Phase 10 · VERIFY · planner · PASS · all 6 p10 suites green; zoom/render-invariant/pinch/gore/ammo/tap-vs-hold confirmed; Phases 3–9 intact; on-device framerate = manual check.

---

Backlog · BQ-3 · expensive · pass · builder construction loop (claim→walk→build→placeStructure spend); resumed uncommitted WIP, 6/6 Done-when green. commit 048ef47.
Backlog · CB-4 · cheap · pass · Plan tool — tap queue/cancel blueprint, drag add-only (fixed toggle-flicker bug); 9/9 + bq3 regression. commit 8711b13.
Backlog · CB-5 · cheap · pass · blueprint overlay render (translucent ghosts, reserved=opaque, ctx-only after putImageData); 9/9 + all p10 render/smoke green. commit 1fd0721.
Backlog · CB-6 · expensive · pass · live-loop integration (resetQueue on world init; overlay+builder confirmed ticking); e2e plan→assign→build→spend via main path 16/16 + regressions. commit a576e25.
Backlog · CB-VERIFY · orchestrator · pass · cooperative base-building playable end-to-end; refreshed stale GATE test p4-t5 (death-model drowning→corpse), GATE green. commit 86ed400.

VS-1 Weather (GDD §10) · T1 · expensive · pass · SNOW material (id16) + §10 config seeds (states/temps/spawn rates); 40/40 + p2-t6. commit 284a7c2.
VS-1 Weather · T2 · expensive · pass · deterministic weather state machine (clear/rain/snow) + ambient temperature; seeded weatherRand, 30k-tick replay identical. commit e3674b0.
VS-1 Weather · T3 · expensive · pass · sky-spawn (rain→WATER/snow→SNOW) + fire douse/snow-melt; chunk byte-equiv held, rain douses 33×, snow no-tunnel. commit e882fb9.
VS-1 Weather · T4 · orchestrator direct · pass · wired warmth need + plant growth onto DYNAMIC weather: survivor cold gate AMBIENT_COLD→isAmbientColdNow() (engine/weather), sapling growth gains GROW_RAIN_SPEEDUP under rain (chunk-safe, global state). Refreshed 2 stale always-cold suites (p12-warmth, p12-seekwarmth) to pin SNOW via __setWeatherForTest. tsc strict clean; 9 suites green (weather ×3, p12-warmth/seekwarmth/warmthbar/grow, p11-chunk-equiv, p2-t6); seekwarmth 20/20 stable.

VS-1 Weather · T5 · orchestrator direct · pass · weather/temperature HUD readout (always-on top-right icon·label·N°) + config-driven precipitation overlay (sky-darken wash + rain streaks / drifting snow flecks via RAIN_STREAK_COLOR/SNOW_FLECK_COLOR/WEATHER_OVERLAY_DENSITY/WEATHER_SKY_DARKEN_*). Wall-clock animated, draw-time only (no sim/RNG/grid touch). tsc strict clean; weather-config/p9-ui/main-smoke/p11-chunk-equiv green; vite build OK. commit 7f9efb4. NOTE: resumed after prior cheap_coder stalled — 7 turns / 12 tool uses but 0 disk changes (looping failed edits).

VS-2 Warmth · status · warmth-half ALREADY DONE (prior p12 warmth pass: needs.warmth, WARMTH_RATE/RESTORE/THRESHOLD, isSheltered, nearWarmth, seekWarmth, freeze→corpse death, warmth HUD bar; VS-1 T4 wired cold gate to dynamic weather). (A) wetness need + WET_WARMTH_MULT ✅ DONE (T-A). (B) graded local effective temp + SNOW_CONTACT_PENALTY/WARMTH_SAMPLE_TICKS ✅ DONE (T-B, commit below). (C) campfire — core (material + sim + warmth) ✅ + rest (placement + seek + wet HUD) ✅ DONE. **🎉 VS-2 WARMTH COMPLETE** (A wetness, B graded effective temp, C campfire).

**CURRENT: VS-3 — survivor-driven cooperative base-building (GDD §6.2/§8/§6.1).** User chose VS-3 (over §14 Tree reproduction) at the VS-2 boundary. Decomposed into: T1 grouping-by-sight (groups.ts) ✅ DONE; T2 shelter.ts (per-group shelter PROJECT = site pick near centroid + enclosed WALL/WOOD box+roof+doorway blueprint sized by members); T3 cooperative build wiring (group builders divide blueprint via existing buildqueue claim/reserve; campfire placed inside on enclose; MAX_BUILD_CLAIMS cap; release on death); T4 split→own shelter + merge→abandon-redundant; T5 main integration + seekWarmth targets group's shelter + e2e Done-when. Reuses existing BQ/CB builder+blueprint machinery + VS-2 warmth/campfire. T1 (groups.ts) ✅ + T2 (shelter.ts) ✅ + T3 (coopbuild.ts wiring) ✅ DONE. **NEXT: T4 (split→own shelter + merge→abandon-redundant).** (T3 surfaced a tall-hut build-reachability gap — see VS-3 RISK below.)

**PLAYTEST v0.8 logged (PLAN.md round 6):** K=can't select a moving survivor / no selection box tracking the sprite (cheap, input+render highlight); L=Assign-menu buttons need colour-matching to ROLE_TINT (cheap, UI); M=snow unbalanced — spawns everywhere at once + buries everything, needs drips-and-drabs/clustered spawn + light/heavy weights + ambient-temperature melt (EXPENSIVE, sim-correctness; applyWeather + new melt rule; re-run p11-chunk-equiv/weather/warmth). **M is unplayable-blocking for the camp loop → land M before/with VS-3 cooperative shelter build.** K/L fold into VS-3 polish.

**v0.8 M DONE (commit below):** snow rebalance. (M-1 spawn) `applyWeather` snow branch rewritten: drifting FLURRY BANDS (SNOW_BAND_WIDTH=14/GAP=70/DRIFT=0.4) of light->heavy intensity (sin on tick, SNOW_INTENSITY_MIN/MAX/FREQ) at in-band rate SNOW_SPAWN_CHANCE=0.06 (was whole-row 0.02) -> ~0.7% coverage, drips not a curtain; rain unchanged. (M-2 ambient melt) updateSnow melts SNOW->WATER when getTemperature()>SNOW_MELT_TEMP(0) at SNOW_AMBIENT_MELT_CHANCE=0.02/tick; chunk-safe via a cold->warm EDGE in step() that calls new chunks.markAllActiveNextTick() to wake a SETTLED snowpack, then warm snow self-marks active until it melts. New SALT_SNOW_AMBIENT_MELT=500, module lastTemp. Done-when test/weather-snow.test.ts (8/8): bands/sparse/drift + accumulate-then-melt-to-0 + **byte-identical chunked-vs-full melt**. Refreshed weather-config (SNOW_SPAWN_CHANCE 0.02->0.06). tsc strict + vite build clean; 75/75 suites green (weather-sim/state/config, p11-chunk-equiv, warmth suites all green). K/L UX fixes pending.

**v0.8 K DONE (commit below):** selection box tracks the sprite. New ui.drawSelectionHighlight(ctx, survivor) draws a dashed yellow box around the selected body each frame (worldToScreen, screen-space, off-screen culled, clears on death); input.getSelectedSurvivor() exposes the role-menu's survivor (auto-closes menu if it died/turned); wired into main render loop after drawWeather. SELECT_TAP_RADIUS 6->12 (forgiving pick of a moving sprite). Done-when test/k-select-highlight.test.ts (6/6): box tracks moving body, none for null/dead, radius widened. 76/76 suites green; tsc strict + vite build clean. **L still pending** (colour-match Assign-menu buttons to ROLE_TINT).

**v0.8 L DONE (commit below):** Assign-menu role colour legend. New roles.roleTintCss(role) -> CSS rgb() from ROLE_TINT (Unassign=neutral grey); input.ts applies it as a left-edge swatch on each data-role button at wire time, so the menu matches the sprite tint and can't drift. Done-when test/l-role-legend.test.ts (8/8): swatches == ROLE_TINT, distinct, Unassign neutral. Also FIXED a pre-existing FLAKY test (test/p12-campseek.test.ts test 2): it ran 6000 ticks and the post-recovery `wander` (Math.random) could drift the survivor out of the lone campfire's scan range and freeze (~intermittent fail; the L sweep surfaced it). Now stops AT recovery (asserts auto-seek + reach + warm above threshold), deterministic 8/8 over repeated runs. **All v0.8 playtest items K/L/M done.** tsc strict + vite build clean; full sweep green.

**v0.8 M round 2 DONE (commit below):** snow STILL buried players+zombies before it could melt (playtest). Two fixes: (1) ~10x lighter - SNOW_SPAWN_CHANCE 0.06->0.006 (light dusting; weather-sim accumulation t500 4802->469). (2) No EXTREME weather early - new WEATHER_SNOW_EARLIEST_TICK=12000; weather.ts redirects a pre-12000 "snow" transition to mild RAIN, so a shelterless early colony isn't buried/frozen. Tests: weather-config (0.006), weather-snow (band check made robust to <1/tick + per-tick shift), weather-state (N 30000->90000; new gate check: no snow pre-12000, snow after; also hardened its duration-bounds check for clear->clear self-repeats + dropped the truncated final run). 78/78 suites green; tsc strict + vite build clean; chunk byte-equivalence intact.

**PLAYTEST v0.9 logged (PLAN.md round 7):** N=stone should obey gravity (fall unless touching another stone "mortared"; STONE id2 isStatic, no rule — EXPENSIVE sim + chunk-equiv). O=survivors drown when rain/melt sheets water over them (balance; largely fixed by Q). P=can't bootstrap wood — assigning a Lumberjack costs wood (the axe), soft-locks at 0 wood → remove the wood cost to assign a lumberjack (CHEAP, roles gating; do FIRST). Q=survivors should FLOAT in water, zombies walk the BOTTOM (per-body buoyancy in locomotion — EXPENSIVE; pair with O). All sim/locomotion-adjacent → re-verify chunk byte-equiv + no-tunnel. P is the cheap high-value unblock; Q+O one locomotion pass; N a separate sim pass.

**v0.9 P DONE (commit below):** wood-bootstrap unblock — the axe is now FREE (`AXE_WOOD_COST` 2→0, config.ts): a wood-costed axe soft-locked the colony at 0 wood (nothing assignable to earn the wood the axe needs). Every other tool keeps its wood gate (pickaxe/weapon/basket/hammer unchanged); `canAfford({wood:0})`/`spend` are safe at zero. Refreshed test/p6-t3 §2 to pin the NEW behaviour (lumberjack assignable at empty stockpile + free craft leaves wood at 0) and moved the wood-gate assertions onto the miner/pickaxe. p6-t3 + p6-t4 + p-bq2-builderrole + main-smoke green; tsc strict + vite build clean.

**USER DECISION (2026-07-05): track = P → shelter-geometry pass → T4.** P ✅ done (above). **NEXT: shelter-geometry pass to resolve the VS-3 RISK** (tall-hut build reachability — pick among options a–d below, lean (c) low lean-to roof or (a) builder-only construction reach), THEN VS-3 T4 (split→own shelter + merge→abandon-redundant). Playtest Q+O (buoyancy/drowning locomotion pass) and N (stone gravity) deferred until after.

**VS-3 GEOMETRY PASS DONE (commit below) — ⚠️ RISK RESOLVED.** Option (a): builders place from BELOW with a taller CONSTRUCTION reach. New `BUILDER_REACH_UP = SHELTER_WALL_HEIGHT` (config, AI-only — no sim rule, no grid semantics); builder call sites (`reachableBlueprint` claim + both `driveBuilder` arrival/validity checks) use it; harvest/consume reach stays BODY_H. Debugging the e2e surfaced the REAL stall (not just reach): `findStandCell`'s score-0 candidate is *standing in the blueprint cell itself* — once a wall stack rises, that wall-top stand always wins, A* can't route to it (step-up 2), and `reachableBlueprint` only tried the single best stand → build stalls at 5 cells AND is a latent self-entombment (place WALL into your own feet). Fixed with builder-specific `findBuildStands` (never stands in the build cell; returns candidates BEST-FIRST so the route check falls back to a ground stand; attempts still capped at REACH_MAX_PATH_ATTEMPTS). **Done-when (test/vs3-buildreach.test.ts, 11/11):** reach invariant covers roof height · 3 builders FULLY enclose the 27-cell hut e2e (tick 1121, roof-CENTRE cell included) · campfire queued+lit on enclose · interior isSheltered · forager still can't harvest foliage 15 up (builder-only). tsc strict + vite build clean; **FULL SWEEP 80/80 suites green.**

**VS-3 T4 DONE (commit below):** split→own shelter + merge→abandon-redundant (coopbuild reconcile). `updateCoopBuild` now RECONCILES first: every project whose owning group id is no longer active (merge absorbed it, members all died/turned, or the canonical min-index id migrated) is abandoned — `clearGroupBuild` cancels its UNRESERVED queued cells, then `clearShelterProject` drops the record; RESERVED cells stay for their builder (finish or release-on-death, no cancel-out-from-under). SPLIT needs no special case — the forked group id gets its own project (own centroid site) on the next ensure; the surviving/merged group KEEPS its existing project as-is (re-planning a half-built hut for the new headcount would thrash the queue; growth re-sizing = later polish). Also refreshed the stale coopbuild header (tall-hut reachability limit → resolved by the geometry pass). **Done-when (test/vs3-t4-splitmerge.test.ts, 18/18):** split forks a second group whose project B sites near ITS members while A keeps streaming · remerge drops B's record + cancels its queued cells · a reserved B cell survives the reconcile (still reserved) · extinct group (all dead) abandons project + empties queue. tsc strict + vite build clean; **FULL SWEEP 81/81 suites green.**

**VS-3 T5 DONE (commit below) — 🎉 VS-3 COMPLETE (T1 groups + T2 shelter + T3 wiring + geometry pass + T4 split/merge + T5 main-loop integration).** (a) MAIN WIRING: `updateGroups(survivors, tickCount)` every tick (self-gates on GROUP_RECHECK_TICKS) + `updateCoopBuild(survivors)` on the SAME cadence, placed after updateInfection (downed/turned already excluded) and before resolveBreaching; `resetGroups()`/`resetShelters()` on world (re)init beside resetQueue. (b) GROUP ID ON THE SURVIVOR: groups.ts recompute now STAMPS `s.groupId` (-1 dead/turned/ungrouped) so survivor-only consumers can find their group without the colony index. (c) SEEKWARMTH TARGETS THE GROUP'S SHELTER: new `groupShelterTarget` in survivor.ts — the group's OWN hut is a KNOWN destination (project lookup by s.groupId, interior feet-row candidates outward from interior.x, only once isShelteredAt i.e. roof on, A*-routed) and `nearestReachableWarmth` prefers it over the generic sheltered-cell/campfire scans (group cohesion; also works beyond scan range while the split debounce holds membership); falls back to the old pick when no home hut. **Done-when (test/vs3-t5-mainloop.test.ts, 25/25):** (A) REAL main.ts under the main-smoke DOM stubs with hand-driven rAF + controlled performance.now — spawn cluster groups on the live path, owns a project, cells stream into the queue (capped, no stray blueprints); (B1) phase e2e in main's tick order: plan → 2 builders raise the hut to FULL enclose (tick ~1712) → campfire lit on enclose → snow cold-snap → civilians recover above threshold inside, nobody freezes; (B2) a cold straggler OUTSIDE walks IN through the doorway and recovers even though a FOREIGN canopy is strictly nearer (pre-T5 behaviour picked the canopy). tsc strict + vite build clean; **FULL SWEEP 82/82 suites green** (incl. fresh-graph suites p11-rng/chunk-equiv, p12-grow/warmthbar, weather-sim/snow). NOTE for coders: a test tsconfig whose test `require('../src/main')`s at RUNTIME must include `"../src/**/*.ts"` (tsc can't follow a runtime require — a stale .test-out/src/main.js otherwise runs silently).

**VS-3 status:** ✅ COMPLETE (T1–T5 + geometry pass). **NEXT (user-ordered): playtest v0.9 Q+O (survivor buoyancy / zombies walk the bottom / drowning balance — one locomotion pass), then N (stone gravity with "mortared" support rule — sim pass, re-verify chunk byte-equivalence + no-tunnel).**

**v0.9 N DONE (commit below):** stone gravity with a "mortared" support rule. STONE (id 2) keeps `isStatic` (never displaceable by other fallers, solid to bodies) but gains its own sim rule `updateStone`: a cell is MORTARED — and therefore holds exactly as before — while ANY 4-neighbour is STONE **or WALL** (a wall IS mortared stone), so worldgen strata, mined galleries, built walls and landed piles never collapse; only a LONE stone obeys gravity, falling STRAIGHT down via the shared `trySwap` (blocky — no diagonal spill/repose; displaces AIR/fluids only, rests on powders). Deterministic (no RNG), settled chunks stay settled (a resting stone touches nothing), and removing a support/mortar is a grid edit that wakes the chunk — the standard dirty-rect contract. **Done-when (test/n-stonegravity.test.ts, 17/17):** lone stone falls straight down, lands, re-mortars, mass conserved, no-tunnel · suspended pair + column hold · WALL counts as mortar (air below) · rests supported on DIRT and ON SAND (never displaces a powder) · sinks through a water pool to its floor · chunked: settle→edit-out support→wakes→drops · settled mortared world = 0 active chunks. Added scenario (f) STONE gravity to the p11-chunk-equiv battery — **6 scenarios byte-identical** chunked vs full at ticks 50/150/300. tsc strict + vite build clean; FULL SWEEP 84/84 suites green.

**v0.9 Q+O DONE (commit below):** per-body buoyancy in locomotion. Two new Body flags (body.ts, both defaulting to the PRE-Q behaviour so raw test bodies are untouched): `buoyant` (createSurvivor sets true) and `breathes` (createZombie sets false; `reanimateAsZombie` flips BOTH on the wrapped body — a turned survivor sinks and stops breathing). locomotion.ts: (1) drown clock now gated on `breathes` (undead never drown → a zombie crosses a lake BED intact — the Q "zombies walk the bottom" half is their existing sink behaviour, now survivable); (2) new buoyancy resolve between the walk and the fall — head-in-WATER → rise 1 whole cell/tick toward the surface (swept, never into solid; if a ceiling blocks the rise the drown clock keeps running, so trapped drowning stays lethal), head-clear-but-torso-in-WATER → the water SUPPORTS the body (skip the fall) → the float line settles with the head above the waterline, which is the O fix (rain/melt sheets no longer drown the colony). Body-layer only — no grid writes, no RNG → chunk/replay-safe by construction (p11-chunk-equiv re-run green anyway). **Done-when (test/q-buoyancy.test.ts, 33/33, static-grid deterministic):** survivor floats up (head dry over 3×DROWN_TICKS, stable ±0, no-tunnel) · zombie sinks, grounds on the pool floor, WALKS the bottom 48 cells with drownTicks pinned at 0 · sealed flooded box still drowns a buoyant survivor at DROWN_TICKS into a prone corpse · flood-sheet scenario lifts a standing survivor off its feet alive · raw createBody sinks+drowns exactly as before (p4-t5 contract) · reanimate flips the flags. Also FIXED a fresh FLAKE in vs3-t5-mainloop B2 (surfaced by this sweep, not a Q regression): B2 pinned snow AND ran simulation.step, so sky-snow drifted on the GLOBAL sim tick — which varies run-to-run with B1's Math.random wander — and a drift landing in the hut doorway blocked the walk-home path (straggler fell back to the canopy). B2's tick now skips the CA (`sim: false` — the seek under test never needs it); 3/3 repeat runs green. tsc strict + vite build clean; FULL SWEEP 83/83 suites green.

**§9 TREE REPRODUCTION DONE (Beyond item 2, commit below) — orchestrator direct · attempt 1 · pass.** GDD §9's two missing halves on top of the existing sapling growth: (1) REPRODUCE — new `applyReproduction()` in simulation.ts, a slow-cadence (`REPRO_INTERVAL`=600t) deterministic NON-chunk-gated sweep (the applyWeather pattern, runs before beginTick): each MATURE plant top (contiguous FOLIAGE column ≥ `REPRO_MIN_HEIGHT`=3, open to the sky — a SNOW cap mutes it) rolls `REPRO_CHANCE`=0.15 to drop a seed `SEED_MIN_DIST..SEED_MAX_DIST`(5..12) columns away (one salt-601 roll packs distance+side); the seed germinates into a SAPLING only on open DIRT with no FOLIAGE/SAPLING within `PLANT_MIN_SPACING`=4 columns (crowding cap → renewable-but-manageable, forests spread but never carpet; SEED_MIN_DIST > spacing keeps the parent out of its own child's box). Worldgen trees qualify — natural woodland reproduces without player planting. (2) SNOW SLOWS/KILLS — under 'snow' weather NO seeds drop and `updateSapling` PAUSES the countdown (slot rewritten + markCellActive every paused tick so the chunk NEVER settles mid-pause — otherwise the chunked scan would sleep through the thaw and diverge); a sapling with an orthogonal SNOW neighbour withers to AIR at `SAPLING_SNOW_KILL_CHANCE`=0.005/tick (salt 700); grown FOLIAGE stays hardy. All randomness simRand-pure; salts 600/601/700. SAPLING already permeableToBodies → autonomous germination can't trap characters. **Done-when (test/eco-repro.test.ts, 6/6):** lone mature tree on a dirt plain → 7 stems after 40 rounds incl. gen-1 child at legal distance + second-generation spread to 24 cols, all pairs > PLANT_MIN_SPACING · pinned snow → 0 new plants over 3600t + parent foliage survives · shielded sapling's countdown holds EXACTLY across 700 snow ticks then resumes+matures on thaw · snow-touching sapling withers (never matures) · chunk byte-equivalence OFF==ON at 5 checkpoints spanning 2 repro rounds (clear) AND the winter pause+kill+sky-snow path. tsc strict + vite build clean; **FULL SWEEP 86/86 suites green** (incl. p12-grow, weather-sim/snow/state/config, p11-chunk-equiv/rng, vs3-t5-mainloop, q-buoyancy, n-stonegravity, r8-bugs). NOTE: p12-grow passes unchanged (its plant's repro rolls happen to fail for the current seed — if REPRO_* rebalancing ever flips it, refresh its scene with the eco-repro stone-cap pattern rather than loosening assertions).

**PLAYTEST v0.10 ROUND 8 (user, 2026-07-05) — ALL 9 ITEMS DONE across 4 commits.** Items: rain floods+drowns everyone · snow buries everyone then melt-floods · drowning mis-reported as zombie kill · wet campfire unaffected · shelter builds floating over the starter camp · shelters need zombie-proof doors · building slow · "lumberjacks get wood without trees" · do survivors eat stored food? · deaths too fast. **(1) R8 bugs commit:** Body.deathCause set by layDownCorpse + state.ts cause chain survivor→body→'killed by zombies' (drown now reports DROWNED, r8-bugs 14/14); shelter.ts sites the hut via a DOWNWARD surface scan from the group's feet (top-down scan saw the starter-camp ROOF → floating hut); lumberjack harvest gate PINNED (zero foliage → zero wood ever — the playtest sighting is off-screen/buried foliage within RESOURCE_SCAN_RADIUS=200, not conjured wood). **(2) R8 economy commit:** stockpile MEALS — a hungry survivor with no wild foliage in range walks to the stockpile point and eats a stored ration (atomic spend({food:1}) via the normal consume flow, consumeCell=null; wild bushes preferred; famine still kills) — ANSWER: foragers' stored food now feeds the colony (it previously did NOT); BUILD_TICKS 120→40; HUNGER/THIRST rates halved (0.005/0.0075 — deaths from crises, not the clock). r8-economy 16/16. **(3) R8 weather commit:** RAIN_SPAWN_CHANCE 0.03→0.004 + per-column RAIN_MAX_POOL_DEPTH=3 cap (wave-proof columnWater probe) + clear-weather EVAPORATION of sky-exposed surface water (EVAPORATE_CHANCE=0.001/column/tick — the missing drainage valve; roofed/underground water and basin ponds persist); SNOW_SPAWN_CHANCE 0.006→0.003 + SNOW_MAX_DEPTH=4 per-column cap (knee-deep blanket, never entombment) + ambient melt yields WATER for only SNOW_MELT_WATER_FRACTION=0.3 of cells (thaw dampens, doesn't flood); WATER douses CAMPFIRE→ASH in reactions.reactCell (start-of-tick grid, scan-order-proof; WATER only — snow never kills a hearth). New salts 800/810; all deterministic; chunk equivalence green (p11 6-scenario + eco-repro batteries). r8-weather 17/17; weather-config pins refreshed. **(4) R8 door commit:** DOOR material id 18 (permeableToBodies for the LIVING; new Body.undead flag — set by createZombie/reanimateAsZombie — makes it SOLID to zombies in bodyCellsSolidAt; breaching probe counts a DOOR as a blocking chippable structure, DOOR_INTEGRITY=80 between fence/wall; flammable — fire remains valid siege counterplay; static+non-displaceable → doors seal out weather/water). STRUCTURES gains 'door' (DOOR_COST 2 wood, toolbar button added); shelter.ts fills the doorway with SHELTER_DOORWAY_HEIGHT 'door' cells so every coop hut ships zombie-barred. r8-door 22/22 (living pass · undead barred · attack-state mob gnaws through in ~5k t (buys time, not a bunker) · hut door: living in/sheltered, zombie out · water sealed · costed placement). Refreshed vs3-shelter/coopbuild/buildreach/t5 kind→material maps + p8-t1 (MATERIALS 19). tsc strict + vite build clean; **FULL SWEEP 89/89 suites green.**

**⚠️ LATENT (pre-existing, discovered while building eco-repro's equivalence scene): `updateDirt` chunk-equivalence gap.** A DIRT cell whose SALT_SPILL roll FAILS returns without self-marking, so a chunk can settle while a dirt cell still has an OPEN diagonal; the full scan keeps rolling every tick and eventually spills it → chunked-vs-full DIVERGE. Minimal repro ON HEAD (pre-§9): bare 160-col DIRT bed on a wider STONE shelf, 100 ticks → diverges at the bed edge (ref=AIR, chk=DIRT). Existing suites never see it because their dirt scenes settle fully (every diagonal blocked) before chunks sleep; worldgen terrain is contiguous so edges are rare in play. NOT fixed here: the naive fix (self-mark while a diagonal is open) would keep every worldgen hillside chunk awake forever AND let all slopes creep flat on the full path — a sim-correctness + perf trade that needs its own task (route expensive_coder if picked up). eco-repro sidesteps it with stone bed-caps (documented in the test).

**⚠️ VS-3 RISK surfaced in T3 — tall-hut build reachability (blocks the phase Done-when #1; resolve in T4/T5 or a geometry pass).** The shelter geometry from T2 cannot be *physically* raised by the current builder pathing. Math: isSheltered needs a roof that clears the head → roof sits 13..18 cells above the floor (WALL_HEIGHT=16, BODY_H=12, ROOF_SCAN=6); but a builder standing on the floor reaches only BODY_H=12 cells up (cellWithinReach/findStandCell). So the roof + top ~3 wall cells are unreachable from the floor, and roof-CENTRE cells (over interior air, with nothing to stand on) are unreachable even from wall-tops. T3 streams bottom-up so walls raise first, but the hut never fully encloses → the campfire-on-enclose + huddle-to-recover e2e can't complete as-is. Options for T5/geometry pass: (a) builders place from BELOW with a taller *construction* reach (relax cellWithinReach for builders only), (b) auto-scaffold / temporary stand cells, (c) a low lean-to roof (single-slope, edge-supported, no centre span), or (d) roof at exactly head-height with a shallower head-clearance invariant. Flagged, not fixed — T3 is wiring only.
VS-2 Warmth · T-A · expensive · attempt 1/2 · TIMED-OUT FAIL · looping failed Edits on config.ts — ~45min, 0 disk writes (working tree stayed clean) while UI showed "editing…". Killed by user. Root cause = non-ASCII look-alikes in src/ (AGENTS.md rule 8); fixed in commit b1415ef.
VS-2 Warmth · T-A · orchestrator direct · attempt 2 · pass · WETNESS need (GDD §6.1 "wet" half of cold-and-wet). Added per-survivor `wetness` float [0,NEED_MAX] (top-level, NOT in `needs` — never reaches the need-at-zero death loop, so wetness alone never kills). Rises by `WETNESS_RATE` in rain (unless roofed) + on WATER/SNOW footprint contact (`inWaterOrSnow` probe); dries by `DRY_RATE` slowly, `×DRY_FIRE_MULT` near a fire. Cold warmth-drain now `×wetMult` where `wetMult = 1 + (WET_WARMTH_MULT−1)·(wetness/NEED_MAX)` (dry=1×, soaked=2.5×). Pure per-survivor state — touches no grid cell, no RNG → chunk/replay-safe. Config seeds: WETNESS_RATE, DRY_RATE, DRY_FIRE_MULT, WET_WARMTH_MULT, WET_ICON_THRESHOLD. Also fixed a stray literal `\n` comment artifact from the ASCII pass. **Done-when (test/p12-wetness.test.ts, 4/4):** rain soaks (0→18 in 300t) · clear dries (100→94 in 400t) · soaked drains warmth exactly 2.50× faster in equal cold (== WET_WARMTH_MULT) · soaked+warm survives 3000t (no direct kill). Refreshed stale always-cold suite **p12-camp** (pinned weather to snow, same T4 fix applied to warmth/seekwarmth — it was failing on HEAD too, not a T-A regression). tsc strict clean; vite build OK; 20 suites green (p12-warmth/seekwarmth/warmthbar/camp/campflow/shelter/grow/corpse/bite/turn, weather ×3, p11-chunk-equiv/rng, p5-t3/t4, p4-t5, main-smoke, p12-wetness).

VS-2 Warmth · T-B · orchestrator direct · attempt 1 · pass · GRADED local effective temperature (GDD §6.1/§10/§13) replaces the boolean cold-gate. New `effectiveTemp(body)` (exported) = ambient (VS-1 `getTemperature`) + `FIRE_WARMTH_BONUS` (near fire) + `SHELTER_WARMTH_BONUS` (roofed) − `SNOW_CONTACT_PENALTY` (footprint touching WATER/SNOW). Sampled per-survivor on a `WARMTH_SAMPLE_TICKS`(=30) interval and cached (smpEffTemp/smpWarm/smpSheltered/smpWetContact + lastWarmthSample) — the ring/roof/contact probes now run every 30 ticks, not every tick (perf, §13). Warmth drains when `effTemp < COLD_THRESHOLD`, **faster the colder it is**: `coldFactor = clamp((COLD_THRESHOLD−effTemp)/WARMTH_COLD_SPAN, MIN, MAX)`, then `×wetMult` (T-A). **Calibration trick:** `WARMTH_COLD_SPAN = COLD_THRESHOLD − TEMP_SNOW = 13` makes coldFactor **exactly 1.0 at pure snow exposure**, so every existing snow-pinned warmth suite sees identical drain — only contact penalty + sample-interval are new behaviour. Wetness kept as the T-A drain multiplier (NOT folded into effTemp) to avoid double-count. `selectBehaviour` left LIVE (responsive override); only the every-tick deplete hot-path is cached. Config seeds: WARMTH_SAMPLE_TICKS, FIRE_WARMTH_BONUS(20), SHELTER_WARMTH_BONUS(18), SNOW_CONTACT_PENALTY(6), WARMTH_COLD_SPAN(13), WARMTH_COLD_FACTOR_MIN(0.4)/MAX(2.5). Removed now-unused `isAmbientColdNow` import (still exported from weather.ts; gate now WEATHER_ENABLED && effTemp<COLD_THRESHOLD). **Done-when (test/p12-efftemp.test.ts, 8/8):** invariants (fire/shelter clear COLD_THRESHOLD; span calibrated) · composition exposed=−8 / +fire=12 / +shelter=10 / −contact=−14 · graded drain contact-vs-snow ratio 1.46 (== coldFactor ratio) · interval caching (fire added mid-window unseen until resample 30t later). tsc strict + vite build clean. Also refreshed stale **p8-t1** (MATERIALS.length 15→17 — pre-existing VS-1-SNOW staleness, failed on HEAD too, unrelated to T-B). **44 suites green** across warmth/weather/combat/worldgen/building/coop (full sweep, zero fail markers).

VS-2 Warmth · T-C (core) · orchestrator direct · attempt 1 · pass · CAMPFIRE - managed contained fire (GDD 8/6.1). New CAMPFIRE material (id 17, ember #e0571a, density DENSITY_FIRE, no integrity -> integrity slot reused as fuel). `updateCampfire` sim rule: NO spread + NEVER ignites (the flee/ignition detectors nearestFire/boneTouchesFire/nearFire all key on FIRE only, so a campfire is automatically flee-safe + body-safe), probabilistic slow burn (consume 1 fuel unit @ CAMPFIRE_BURN_CHANCE=0.04/tick -> ~5000-tick life from a Uint8 CAMPFIRE_FUEL=200 seed; auto-seeds on first visit, SAPLING pattern), expires to ASH (slot cleared). markCellActive every tick (keeps chunk live, same as FIRE -> chunk-safe). Warmth detection taught about campfire: nearWarmth now box-scans FIRE||CAMPFIRE so effectiveTemp grants FIRE_WARMTH_BONUS near a campfire - but nearestFire (flee) stays FIRE-only, so survivors warm at, not flee, their hearth. Config: CAMPFIRE_FUEL, CAMPFIRE_BURN_CHANCE; new SALT_CAMPFIRE_BURN=400. Done-when (test/p12-campfire.test.ts, 5/5): effTemp +bonus clears COLD_THRESHOLD; penned cold survivor by a campfire stays warm 4000t, alive, NEVER fleeFire; 0 FIRE + WOOD intact over 400t (no-spread); burns >> FIRE_LIFETIME then -> ASH. Added a CAMPFIRE-next-to-WOOD scenario to p11-chunk-equiv -> byte-identical chunked-vs-full (5 scenarios). Refreshed stale counts p8-t1 (->18) + weather-config (SNOW no longer last). tsc strict + vite build clean; 72/72 suites green. Pending (T-C rest): placement tool, seekWarmth-to-campfire, wet-icon HUD.

VS-2 Warmth · T-C (rest) · orchestrator direct · attempt 1 · pass · campfire PLACEMENT + seek + wet HUD - closes VS-2. (a) PLACEMENT: added 'campfire' to building.ts StructureKind/STRUCTURES (material CAMPFIRE, cost CAMPFIRE_COST={wood:3}); the data-driven Build flow needs nothing else - new index.html toolbar button (data-tool=build data-structure=campfire) places it now, refreshBuildButtons greys it via canPlace. (b) SEEK: seekWarmth now huddles by a campfire OR a roof (was shelter-only) - new nearestReachableWarmth picks the nearer of nearestReachableShelter and nearestReachable(CAMPFIRE) (its standable bank is adjacent => within FIRE_WARMTH_RADIUS => warm); arrival/validity accept campfire-adjacency (new campfireNear helper, CAMPFIRE-only so raw FIRE is still never sought); selectBehaviour unchanged (already exits on nearWarmth, which now counts campfire). (c) HUD: drawNeedsBars draws a blue umbrella glyph (☔, ASCII-safe escape) right of the warmth bar when wetness >= WET_ICON_THRESHOLD*NEED_MAX. Config: CAMPFIRE_COST. Done-when (test/p12-campseek.test.ts, 13/13): placement costed/atomic/refused-when-broke; cold survivor with a reachable campfire (no shelter) auto-seeks it, reaches warmth range, recovers to 78.9 warmth, never freezes; wet glyph drawn only when wet. tsc strict + vite build clean; ASCII-clean; **73/73 suites green** (full sweep). p12-seekwarmth unaffected (no campfire in its scenes -> nearestReachableWarmth == shelter result).

## Blockers

_(none)_

---

## Commit log (phase boundaries)

Phase 0 — commit 8972036.
Phase 1 — commit de42b5c (includes toolchain recovery: pnpm + TOOLING.md).
Phase 2 — commit aca1595.
Phase 3 — commit fc9cd1e.
Phase 4 (THE GATE) — commit b798001.
Phase 5 — commit 9cc4ff2.
Phase 6 t1–t4 + bootstrap fix — commit 6c1d980.
Phase 6 (complete) — commit 611b814.
Phase 7 t1–t7 WIP — commit 8767e9a.
Phase 7 (complete) — commit 90f15c2.
Phase 8 (complete) — commit 1631561.
Phase 9 WIP — commit a0ae3ba; playtest #2/#6/#7 + PLAN — commit 644e32e.
Phase 9 (complete) — commit 8998254 (+ v0.4b/v0.5/v0.6 patches).
Phase 10 (complete) — see commit below.
VS-1 (Weather, complete) — T1–T5 pass; commit 7f9efb4 (T5). Weather cycles, rain douses fire, snow piles/melts, HUD shows weather+temp.
VS-3 Coop-build · T1 · orchestrator direct · attempt 1 · pass · groups.ts - survivor grouping by sight (GDD 6.2/7.1/13). A GROUP is a connected component of the pairwise visibility graph (within SIGHT_RADIUS AND line-of-sight - new DDA lineOfSight blocks on isSolidForBody cells). HYSTERESIS: each pairwise edge (keyed by stable survivor INDEX, not dynamic group id) is debounced - a newly-visible pair turns ON only after MERGE_DEBOUNCE_TICKS continuous sight, a newly-blocked pair OFF only after SPLIT_DEBOUNCE_TICKS; groups = components of the debounced-edge graph (so a drifting sub-cluster forks when its connecting edges go dark, and internal edges keep it one group). Recomputes only every GROUP_RECHECK_TICKS (GDD 13); debounce measured in real ticks (cadence-independent). Canonical group id = min member index. Dead/turned -> group -1. Config: SIGHT_RADIUS(70), GROUP_RECHECK_TICKS(30), SPLIT_DEBOUNCE_TICKS(240), MERGE_DEBOUNCE_TICKS(120). Done-when (test/vs3-groups.test.ts, 19/19): co-located=1 group; far=2; transitive A-B-C chain=1; wall blocks LOS=2; split debounces (forks at t=270); merge debounces (rejoins at t=150); interval gate; dead/turned=-1. tsc strict + vite build clean; ASCII-clean. Reuses nothing yet; T2+ wire it to shelter projects.

VS-3 Coop-build · T2 · orchestrator direct · attempt 1 · pass · shelter.ts - per-group shelter PROJECT planning (GDD 8/6.1). Each group owns at most ONE project (Map<groupId, ShelterProject>): planShelter() centroids the members, finds the surface, and lays out a roofed hut sized by member count - interior area = max(SHELTER_MIN_SIZE, members*SHELTER_PER_SURVIVOR_AREA) -> width; cells = WOOD roof span + full-height left WALL + right WALL with a SHELTER_DOORWAY_HEIGHT(>=BODY_H) gap at the floor so survivors walk in/out (NOT a sealed box - VS-2 open-camp lesson). SHELTER_WALL_HEIGHT(16) constrained so a standing body's head sees the roof within SHELTER_ROOF_SCAN -> built hut passes isSheltered. campfire spot at interior-left. ensureShelterProject (idempotent per group) / getShelterProject / clearShelterProject (T4 abandon) / resetShelters. Pure over (members, grid) -> deterministic. Done-when test/vs3-shelter.test.ts (14/14): sized-by-members + bigger-with-more, centroid-sited, roof/left-wall/doorway geometry, BUILT hut isSheltered (open ground not), one-per-group, deterministic. Config: SHELTER_PER_SURVIVOR_AREA/MIN_SIZE/MIN_WIDTH/WALL_HEIGHT/DOORWAY_HEIGHT. tsc strict + vite build clean; 77/77 suites green. T3 will hand project.cells to the buildqueue for cooperative construction.

VS-3 Coop-build · T3 · orchestrator direct · attempt 1 · pass · coopbuild.ts - cooperative shelter build WIRING (GDD 8/6.1/13). Glue between a group's shelter PROJECT (shelter.ts) and the global builder machinery (buildqueue + survivor builder loop) - owns NO build AI. updateCoopBuild(survivors): for each active group (groups.ts), ensureShelterProject then streamProject. streamProject streams the project's still-unbuilt cells into the ONE global queue capped at MAX_BUILD_CLAIMS(6) live at once (GDD 13 cost discipline), BOTTOM-UP (lowest y first) so the capped pool holds cells reachable NOW; the existing reachableBlueprint reserve mechanism divides the cells among builders for free (no double-claim). On shellComplete (every cell its material) it queues ONE campfire blueprint on the interior floor (VS-2 hearth) - never before. clearGroupBuild(groupId) cancels a group's UNRESERVED queued cells (T4 abandon) but leaves RESERVED ones for their builder. Also closed the BQ-4 TODO: a builder that DIES/TURNS holding a claim now RELEASES it (new releaseBuildClaim in survivor.ts dead/turned guards) so jobs can't orphan/deadlock. Config: MAX_BUILD_CLAIMS(6). Done-when test/vs3-coopbuild.test.ts (22/22): stream+cap+bottom-up, top-up+division (3 builders raise the reachable lower course, pool stays capped), campfire-only-on-enclose (+exactly one, idempotent, builder lights it), release-on-death, abandon-keeps-reserved. tsc strict + vite build clean; ASCII-clean; regression green (p-bq1/2/3, vs3-shelter/groups, p-cb6-coop-e2e, p12-turn, main-smoke). **KNOWN GAP (see VS-3 RISK above): tall-hut roof/upper-wall cells are physically unreachable by current pathing - T3 wiring is correct but the hut can't fully enclose until T5/geometry resolves reach; flagged, not fixed.**

VS-2 (Warmth, complete) — T-A wetness (2e8141a), T-B graded effective temp (7053c9f), T-C campfire core (c4fec5f) + rest (this commit). Survivors get cold AND wet; effective temp = ambient + fire/shelter - contact; a placeable campfire warms a camp (never spreads/ignites/fled); cold survivors huddle by a campfire or shelter and recover, else freeze. 73/73 suites green; chunk byte-equivalence holds (campfire scenario added).
